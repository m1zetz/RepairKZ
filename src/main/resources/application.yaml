spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: repairkz.auth@gmail.com
    password: qxht gvlw iliu yafu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/repair_kz
    username: postgres
    password: "12345678"
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      idle-timeout: 30000
      connection-timeout: 20000
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 10MB
  jpa:
    hibernate:
      ddl-auto: update
      show_sql: true
      properties:
        hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
  application:
    name: RepairKZ-Backend
security:
  key: mySecretKeyForRepairKZApplicationJWT2024
logging:
  level:
    org.springframework.web: DEBUG